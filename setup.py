from setuptools import setup

setup(
        name = 'minimalmite',
        packages=['minimalmite'],
        version='0.0.1a0',
        install_requires=[
            'docopt',
            'msgpack'
        ]
)
